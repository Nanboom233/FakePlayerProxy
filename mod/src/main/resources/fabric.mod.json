{
  "schemaVersion": 1,
  "id": "fakeplayerproxy-mod",
  "version": "${version}",
  "name": "FakePlayerProxy Client Mod",
  "description": "Client acknowledgement for patched FakePlayerProxy Velocity login relay.",
  "environment": "client",
  "mixins": [
    "fakeplayerproxy-mod.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.19.3",
    "minecraft": "=26.2",
    "java": ">=25"
  }
}
